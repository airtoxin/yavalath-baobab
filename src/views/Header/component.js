import React from 'react';
import styles from './component.css';

export default (props) => {
  return (
    <div className={`${styles.noSpace} ${styles.black}`}>
      <h1>Yavalath</h1>
    </div>
  );
};
